apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: dev-nginx
  namespace: flux-system
spec:
  interval: 1m
  path: "./nortura-infra/nginx-app"
  prune: true
  retry:
    limit: 3
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system  
